#include <stdlib.h>
#include <time.h>
/* more headers goes there */

/* betty style doc for function main goes there */
int main(void)
{
	/*Save the number x */
	int x = 0;
	/* Use the for loop  */
	for (x = 0; x < 10; x++)
		printf("%d", x);
	printf("\n");
	return (0);
}

